<h1>Burgers!</h1>

<h2>Burgers Ready to be Eaten</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{!-- {{> burger/burger-block devour=true}} --}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers In Progress</h2>

<ul>
  {{#each burger}}
    {{#if devour}}
      {{!-- {{> devour/devour-block devour=false}} --}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Devour</label><br>
    <input type="radio" name="sleepy" value="1" checked> Devour!<br>
    <input type="radio" name="sleepy" value="0"> Make
  </div>

  <button type="submit">Add Burger</button>
</form>
